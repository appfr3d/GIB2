const config = {
  apiDomain: 'http://geomatikk.herokuapp.com',
  authStorageKey: '@geoMATikk:auth', // Used to save auth object in async storage
};

// Heroku credentials:
// mail: alfredla@stud.ntnu.no
// pass: geoMATikk123!

export default config;
